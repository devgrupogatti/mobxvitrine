import 'package:flutter/material.dart';

class BuscaVB extends StatelessWidget {
  const BuscaVB({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Busca vitrine beauty'),
    );
  }
}
